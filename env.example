# Morning Math Challenge - Environment Variables Template
# 🚀 環境別設定ガイド

# =====================================================
# 📋 必須環境変数 (★) vs 任意環境変数 (☆)
# =====================================================

# === データベース設定 ===
# ★ MongoDB接続文字列 (本番: Atlas URL, 開発: ローカル)
MONGODB_URI=mongodb://localhost:27017/morning_challenge

# ☆ MongoDB モック使用フラグ
# 開発環境: true, 本番環境: false, Vercelビルド: true
MONGODB_MOCK=true

# === セキュリティ設定 ===
# ★ JWT署名用シークレット (最低32文字、本番では強力なランダム文字列)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# ☆ JWTトークン有効期限
JWT_EXPIRES_IN=7d

# === 管理者設定 ===
# ★ 管理者メールアドレス (本番環境でのみ必須)
ADMIN_EMAIL=admin@example.com

# ★ 管理者初期パスワード (本番環境でのみ必須)
ADMIN_DEFAULT_PASSWORD=admin123

# === アプリケーション設定 ===
# ★ Node.js実行環境
NODE_ENV=development

# ☆ 時間チェック無効化 (開発時のみ)
DISABLE_TIME_CHECK=true

# === フロントエンド設定 ===
# ☆ Vite開発サーバーポート
VITE_PORT=3004

# ☆ APIベースURL
VITE_API_BASE_URL=http://localhost:5003/api

# === サーバー設定 ===
# ☆ バックエンドサーバーポート
PORT=5003

# === 本番環境用設定 ===
# ☆ フロントエンドURL (本番のみ)
FRONTEND_URL=https://your-app.vercel.app

# ☆ CORS許可オリジン (本番のみ)
CORS_ORIGIN=https://your-app.vercel.app

# =====================================================
# 🔧 環境別セットアップ手順
# =====================================================

# 📍 ローカル開発環境:
# 1. このファイルを .env にコピー
# 2. MONGODB_MOCK=true のまま (モックDB使用)
# 3. JWT_SECRET は適当な32文字以上の文字列に変更

# 📍 Vercel本番環境:
# 1. Vercel Dashboard > Settings > Environment Variables
# 2. 以下を Production 用に設定:
#    - MONGODB_URI: MongoDB Atlas URL
#    - JWT_SECRET: 強力なランダム文字列 (64文字推奨)
#    - ADMIN_EMAIL: 実際の管理者メール
#    - ADMIN_DEFAULT_PASSWORD: 強力なパスワード
#    - NODE_ENV: production
#    - MONGODB_MOCK: false

# 📍 Vercelプレビュー環境:
# 1. Preview 用に以下を設定:
#    - MONGODB_MOCK: true (モック使用)
#    - NODE_ENV: preview
#    - その他はダミー値でOK

# =====================================================
# ⚠️  セキュリティ注意事項
# =====================================================
# ・このファイルはリポジトリにコミットしないこと
# ・本番環境では .env ファイルを使わず Vercel Environment Variables を使用
# ・JWT_SECRET は十分に強力なランダム文字列を使用
# ・ADMIN_DEFAULT_PASSWORD は初回ログイン後に変更すること 